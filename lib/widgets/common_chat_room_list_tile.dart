import 'package:flutter/material.dart';

class CommonChatRoomListTile extends StatelessWidget {
  const CommonChatRoomListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
